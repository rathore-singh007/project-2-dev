pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                git 'https://github.com/your-username/project-2-dev.git'
                sh 'mvn clean install'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test'
            }
        }
        stage('Build Docker Image') {
            steps {
                script {
                    docker.build('project-2:latest')
                }
            }
        }
        stage('Push Docker Image') {
            steps {
                script {
                    docker.withRegistry('https://index.docker.io/v1/', 'dckr_pat_tSqIg9pWAO4NuU5uk8pdxqsQz7E') {
                        docker.image('project-2:latest').push('latest')
                    }
                }
            }
        }
        stage('Deploy to Kubernetes') {
            steps {
                sh 'kubectl apply -f kubernetes-deployment.yaml'
            }
        }
    }
}
